export const metadata = {
  title: "Support — AZDOME",
  description:
    "Installation guides, firmware, troubleshooting, and contact options.",
};

export default function SupportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
